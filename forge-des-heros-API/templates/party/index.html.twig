{% extends 'base.html.twig' %}

{% block title %}Party index{% endblock %}

{% block body %}
    <h1>Party index</h1>

    <p>
        Filtrer :
        {% if status == 'all' %}
            <strong>Tous</strong>
        {% else %}
            <a href="{{ path('app_party_index', {'status': 'all'}) }}">Tous</a>
        {% endif %}
        |
        {% if status == 'available' %}
            <strong>Avec places disponibles</strong>
        {% else %}
            <a href="{{ path('app_party_index', {'status': 'available'}) }}">Avec places disponibles</a>
        {% endif %}
        |
        {% if status == 'full' %}
            <strong>Complets</strong>
        {% else %}
            <a href="{{ path('app_party_index', {'status': 'full'}) }}">Complets</a>
        {% endif %}
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>MaxSize</th>
                <th>Occupation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for party in parties %}
            <tr>
                <td>{{ party.id }}</td>
                <td>{{ party.name }}</td>
                <td>{{ party.description }}</td>
                <td>{{ party.maxSize is null ? 'Non definie' : party.maxSize }}</td>
                <td>
                    {{ party.characters|length }}
                    {% if party.maxSize is not null %}
                        / {{ party.maxSize }}
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('app_party_show', {'id': party.id}) }}">show</a>
                    <a href="{{ path('app_party_edit', {'id': party.id}) }}">edit</a>
                    {{ include('party/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_party_new') }}">Create new</a>
{% endblock %}
